pipeline {
  agent {
    kubernetes {
        label 'default-java'
        yamlFile 'maven-pod.yml'
    }
  }
  stages {
    stage('Run maven') {
      steps {
        container('maven') {
          sh 'mvn -version'
        }
      }
    }
  }
}
